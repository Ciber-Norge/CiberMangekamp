!!!
%haml
  %head
    %title Ciber Mangekamp

  %body
    %h1
      = @event.title
    = link_to 'Tilbake', season_path(@event.season_id)

    %h1 Add participant
    = form_tag [@event, Participant.new], url: {action: "create"}, html: {class: ""} do
      = collection_select :user, :user_id, @users, :id, :name
      = submit_tag 'Add participant'

    %h1 Participants
    %ul
      - @event.participants.each do | participant |
        %li
          = participant.user.name
          =# link_to "delete", :event_participant, method: :delete, data: { confirm: "You sure?" }

    %h1 Results
    %ul
      - @event.results.each do | result |
        %li
          = result
